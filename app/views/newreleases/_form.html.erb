<%= form_for(@newrelease) do |f| %>
  <% if @newrelease.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newrelease.errors.count, "error") %> prohibited this newrelease from being saved:</h2>

      <ul>
      <% @newrelease.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
