<%= form_for(@newseries) do |f| %>
  <% if @newseries.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newseries.errors.count, "error") %> prohibited this newseries from being saved:</h2>

      <ul>
      <% @newseries.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
