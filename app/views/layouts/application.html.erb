<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title, ) ? yield(:title) : "Rubber City Comics" %></title>

    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>
  
  <body>
    <%= render 'layouts/header' %>
    <hr/>
    <div class="row">
      <div class="whirl traditional" id="whirly">
        <h3>My appologies <%= current_user.f_name %>, This is going to take a moment...</h3>
      </div>
      <div class="large-12 columns">
        <div class="panel">
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
